//! `clove(1)` — control CLI for `cloved`.
//!
//! Speaks to the daemon over the local HTTP API (unix socket default,
//! localhost TCP opt-in). Hand-rolled arg parsing per SCOPE §9; human
//! tables by default, `--json` on every read command. Built in Phase F.

fn main() {
    eprintln!("clove: not implemented yet — bootstrap skeleton (see docs/PLAN.md)");
    std::process::exit(1);
}
